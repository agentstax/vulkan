---
title: "vulkan.consumer.cursor.head"
slug: errors/VK0080
kind: "metric"
consequence: "gauge of `{message}`, one series per consumer group"
---

The highest message id ever appended to the topic.

The `topic` and `group` attributes identify the series. Every group on one
topic sees the same head; it is repeated per group so it can be compared
directly with that group's claimed and committed frontiers.
