---
title: "vulkan.alert.check.published_alerts"
slug: errors/VK0077
kind: "metric"
consequence: "gauge of `{alert}`, one series per alert check"
---

Alerts one run of the named check changed to active.

The `alert` attribute identifies the check. An already-active result does not
count as another change; the value describes the latest check run rather than
a lifetime total.
