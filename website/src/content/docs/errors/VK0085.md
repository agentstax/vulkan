---
title: "vulkan.consumer.exceptions.ready"
slug: errors/VK0085
kind: "metric"
consequence: "gauge of `{exception}`, one series per consumer group"
---

Delivery rows waiting for their retry delay to pass.

The `topic` and `group` attributes identify the series. Once `can_run_after`
passes, an exception consumer can claim the row for another attempt.
