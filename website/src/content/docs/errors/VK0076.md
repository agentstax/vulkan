---
title: "vulkan.alert.check.topics_failed"
slug: errors/VK0076
kind: "metric"
consequence: "gauge of `{topic}`, one series per alert check"
---

Topics whose evaluation or alert-result production failed during one run of
the named alert check.

The `alert` attribute identifies the check. Compare this with
[`vulkan.alert.check.topics_evaluated`](/errors/VK0075) from the same retained
run to see how much of that pass failed.
