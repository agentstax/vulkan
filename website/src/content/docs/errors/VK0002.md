---
title: "lifecycle context can never be cancelled"
slug: errors/VK0002
---

**Code** `VK0002` · **Recovery** permanent -- an unchanged retry cannot succeed; retry machinery stops immediately.

## Fix

pass the application's shutdown context, or set the config's DisableGracefulShutdown
