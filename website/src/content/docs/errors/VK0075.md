---
title: "vulkan.alert.check.topics_evaluated"
slug: errors/VK0075
kind: "metric"
consequence: "gauge of `{topic}`, one series per alert check"
---

Topics one run of the named alert check evaluated.

The `alert` attribute separates partition count, compaction read cost, and
worker liveness. Each run replaces that check's retained value; this is not a
cumulative counter.
