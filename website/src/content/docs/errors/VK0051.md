---
title: "vulkan.consumer.session.lease_lost"
slug: errors/VK0051
---

**Code** `VK0051` · **Metric** -- counter of `{commit}`, one series per consumer session.

Commits rejected because another worker had reclaimed the lease first.
This instance's work on those rows was redone elsewhere; nonzero means
this process was too slow to record what it ran.

A session flow: this instance's own running total, flushed to
`__system.metrics` each flush tick and printed as `lease_lost_count` on
the [consumer stopped](/errors/VK0041) line. It never reconciles against
the fleet-wide `vulkan.consumer.*` gauges.
