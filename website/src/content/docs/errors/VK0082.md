---
title: "vulkan.consumer.cursor.committed"
slug: errors/VK0082
kind: "metric"
consequence: "gauge of `{message}`, one series per consumer group"
---

The frontier at or below which every message is complete or dead for the
consumer group.

The `topic` and `group` attributes identify the series. Ready, inflight, or
deferred exception rows keep this frontier below their message ids until they
resolve.
