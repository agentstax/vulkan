---
title: "vulkan.consumer.exceptions.inflight"
slug: errors/VK0086
kind: "metric"
consequence: "gauge of `{exception}`, one series per consumer group"
---

Delivery rows currently held by a retry attempt.

The `topic` and `group` attributes identify the series. These are exception
rows with an active lease, separate from the cursor range represented by
[`vulkan.consumer.cursor.inflight`](/errors/VK0084).
