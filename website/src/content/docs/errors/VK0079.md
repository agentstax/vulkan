---
title: "vulkan.topic.state.compacted"
slug: errors/VK0079
kind: "metric"
consequence: "unitless gauge, one series per topic"
---

Whether the topic has received a keyed message that opted into compaction.

The `topic` attribute identifies the topic. `1` means the topic snapshot found
compaction state; `0` means it did not. This reports topic state, not how many
messages were compacted.
