# TODO

Sliding window of in-flight work only. Future work lives in ROADMAP.md;
shipped work in HISTORY.md; decision rationale in DECISIONS.md ->
docs/decisions/.

## DDL table design diagram (doc site)

Built 2026-08-28 as concepts/table-design — the last content-owed item
from ROADMAP Now. One ASCII topology diagram (control plane, family,
the id-names-the-family seam, the three FK crossings) plus per-table
structural facts (PKs, partitioning, the exactly-one-owner CHECK) and
a "deliberate absences" section; depth stays above column lists so the
page can't silently drift from the DDL. Board row added; inbound links
from architecture, quickstart step 5, routing. Awaiting user review;
on approval the emptied "content still owed" ROADMAP bullet comes out.
