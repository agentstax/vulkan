# Decision records

One line per record: number, date, status, title. Grep here first, then open
only the records you need. Bodies live in `docs/decisions/NNNN-<slug>.md`.

- Numbers were allocated in per-phase blocks at extraction time, so gaps are
  normal and numeric order is only approximate chronology — sort by the date
  column for true order. Records 0501-0510 belong to the Phase 13 review
  (2026-07/08) despite their high numbers.
- A new record takes the next number after the current maximum. Records are
  append-only: changing a decision means a new record plus flipping the old
  one's status to `superseded` and naming the successor in both bodies.

- 0001 2026-06-13 accepted (phase 1) — Claims use SELECT ... FOR UPDATE SKIP LOCKED
- 0002 2026-06-13 accepted (phase 1) — The message DELETE commits in the same transaction as the claim
- 0003 2026-06-13 superseded (phase 1) — The row lock is held for the entire processing duration
- 0004 2026-06-13 accepted (phase 1) — Batch limit pinned to 1
- 0005 2026-06-13 accepted (phase 1) — Graceful shutdown lets the in-flight batch finish under context.WithoutCancel
- 0006 2026-06-13 accepted (phase 1) — The table is named message_log, not jobs
- 0021 2026-06-14 accepted (phase 1.5) — The queue lives in the same Postgres database as the business data
- 0022 2026-06-14 accepted (phase 1.5) — AppendMessage threads its transaction into a producer callback
- 0023 2026-06-14 accepted (phase 1.5) — ProducerFunc takes a concrete pgx.Tx
- 0041 2026-06-15 accepted (phase 2) — The claim is row data (status + locked_at), not a held DB lock
- 0042 2026-06-15 accepted (phase 2) — Failures return to ready with a backoff on can_run_after; max attempts goes dead
- 0043 2026-06-15 accepted (phase 2) — The dead-letter queue is WHERE status='dead', not a separate table
- 0044 2026-06-15 accepted (phase 2) — Lease reclamation is a claim-predicate branch, not a reaper daemon
- 0045 2026-06-15 accepted (phase 2) — The stuck window is the work timeout plus a 5s buffer
- 0046 2026-06-15 accepted (phase 2) — The consumer owns the operational knobs; the datastore takes only connection params
- 0047 2026-06-15 accepted (phase 2) — Delivery is at-least-once; consumerFunc must be idempotent
- 0061 2026-06-20 accepted (phase 3) — The prefetcher blocks on WaitForRoom; the buffer never drops a claimed row
- 0062 2026-06-20 accepted (phase 3) — The in-memory buffer stays shallow: depth is a lease-safety constraint, not a throughput lever
- 0063 2026-06-20 accepted (phase 3) — Claim per batch, record success or failure per message
- 0064 2026-06-20 accepted (phase 3) — Throughput is min(supply(batch), ack_capacity(workers))
- 0065 2026-06-20 accepted (phase 3) — The claim index is a partial index on id covering ready and processing
- 0066 2026-06-20 accepted (phase 3) — MaxConns is set explicitly on the datastore pool
- 0067 2026-06-20 accepted (phase 3) — Scaling levers are applied in a fixed order
- 0081 2026-06-20 accepted (phase 3.5) — Measure synchronous_commit only; skip the batch-ack measurement
- 0082 2026-06-20 accepted (phase 3.5) — synchronous_commit=off is safe for this queue: it adds no new failure mode
- 0083 2026-06-20 accepted (phase 3.5) — The crash lab proves off's cost is duplicate reruns, not loss, by SIGKILL with a widened WAL writer window
- 0084 2026-06-20 accepted (phase 3.5) — The on/off sweep is read by shape with best-of-3/max, and conc=1 is the number to trust
- 0085 2026-06-20 accepted (phase 3.5) — synchronous_commit=local is not measured
- 0086 2026-06-20 accepted (phase 3.5) — The knob is set via ALTER DATABASE, and reset to on after the sweep
- 0101 2026-06-23 accepted (phase 4) — Consuming reads an append-only log through a per-group cursor instead of mutating message rows
- 0102 2026-06-23 accepted (phase 4) — The cursor claim must `ORDER BY id`; an unordered claim silently loses messages
- 0103 2026-06-23 accepted (phase 4) — The cursor advances after each message, not once per batch
- 0104 2026-06-23 superseded (phase 4) — `MoveCursor` stays a bare `SET position = $1` with no monotonic guard
- 0105 2026-06-23 accepted (phase 4) — `FOR UPDATE` on the cursor serializes concurrent claims only, not the process-then-advance window
- 0106 2026-06-23 accepted (phase 4) — Dropped lifecycle and lease machinery is parked as reference, not deleted
- 0107 2026-06-23 accepted (phase 4) — Claimed rows are drained with `CollectRows` before the claim transaction commits
- 0121 2026-06-23 accepted (phase 5) — A newly registered consumer group starts at position 0 and replays retained history
- 0122 2026-06-23 accepted (phase 5) — `Process` is a continuous poll loop, not a single-shot pass
- 0123 2026-06-23 accepted (phase 5) — The codebase keeps `message_log`/`cursor`/`position` naming rather than renaming to events/consumers
- 0124 2026-06-23 superseded (phase 5) — Fan-out does not need a monotonic cursor guard; `MoveCursor` stays `SET position = $1`
- 0125 2026-06-23 accepted (phase 5) — A `consumerFunc` error stops the whole poll loop; per-group retry and DLQ are deferred
- 0126 2026-06-23 accepted (phase 5) — The poll loop waits one interval before its first claim
- 0141 2026-06-26 accepted (phase 6.5a) — Two cursor frontiers (`claimed`, `committed`) carry the happy path with no per-message row
- 0142 2026-06-26 accepted (phase 6.5a) — `committed` advances after each message, not once per batch at `high`
- 0143 2026-06-26 accepted (phase 6.5a) — `MoveCursor` gains the monotonic guard `WHERE committed < $1`
- 0144 2026-06-26 accepted (phase 6.5a) — The claim reads the pre-update `claimed` via an `old_values` CTE, not PostgreSQL 18's `old` alias
- 0145 2026-06-26 superseded (phase 6.5a) — `claimed` advances at claim time, before processing, with no lease
- 0161 2026-06-30 accepted (phase 6.5b) — Crash recovery rides on a lease per claimed range, not per-message rows
- 0162 2026-06-30 accepted (phase 6.5b) — AdvanceWaterline is two statements, not one UPDATE
- 0163 2026-06-30 accepted (phase 6.5b) — A lease covers (low, high], matching the claim read's convention
- 0164 2026-06-30 accepted (phase 6.5b) — Reclaim drains before fresh claim, one expired lease per poll
- 0165 2026-06-30 accepted (phase 6.5b) — The cap on repeated reclaims was deferred until the exception path existed
- 0166 2026-06-30 accepted (phase 6.5b) — Waterline advance moved to a lazy roller; per-message MoveCursor deleted
- 0181 2026-07-03 accepted (phase 6.5c) — A deliveries row exists only while a message needs individual attention; success is no row
- 0182 2026-07-03 accepted (phase 6.5c) — Commit frees the lease before recording exception rows
- 0183 2026-07-03 accepted (phase 6.5c) — The waterline pins below unresolved exceptions; dead rows do not block it
- 0184 2026-07-03 accepted (phase 6.5c) — ClaimExceptions dead-letters crash-looping rows before claiming
- 0185 2026-07-03 accepted (phase 6.5c) — DrainExceptions runs as its own poll loop, separate from CursorClaim
- 0186 2026-07-03 accepted (phase 6.5c) — MessageException and MessageTerminal are two distinct types, not a flag
- 0187 2026-07-03 accepted (phase 6.5c) — No ON CONFLICT on the exception INSERT
- 0188 2026-07-03 accepted (phase 6.5c) — Resolution verbs are RecordExceptionSuccess/RecordExceptionFailure, not Ack/Nack
- 0189 2026-07-03 accepted (phase 6.5c) — String building in SQL uses concat(), not ||
- 0190 2026-07-03 accepted (phase 6.5c) — Reclaim is one atomic UPDATE, not DELETE plus INSERT
- 0191 2026-07-03 accepted (phase 6.5c) — A range past MaxRangeReclaims moves into the per-message exception path
- 0192 2026-07-03 accepted (phase 6.5c) — ExceptionClaim payload unmarshal failure is fatal, not retried
- 0201 2026-07-03 accepted (phase 7) — Producers publish a routing_key attribute; groups opt in via binding rows; no binding means receive everything
- 0202 2026-07-03 accepted (phase 7) — Routing is one shared predicate evaluated at read time in both consume paths
- 0203 2026-07-03 accepted (phase 7) — Binding patterns are true wildcards, not NATS-style depth-precise matchers
- 0204 2026-07-03 accepted (phase 7) — BindTopic and ClearBindings are admin calls, not Datastore interface methods
- 0205 2026-07-03 accepted (phase 7) — Reads name their columns explicitly; SELECT * is out
- 0206 2026-07-03 accepted (phase 7) — Matching is on routing_key only; no header or content matcher
- 0207 2026-07-03 accepted (phase 7) — binding.kind, header_match, and their CHECK constraint were dropped
- 0208 2026-07-03 accepted (phase 7) — FanOut keeps its full-table rescan; a per-group high-water mark is deferred
- 0221 2026-07-08 accepted (phase 8a) — message_log is partitioned by RANGE (id), not created_at
- 0222 2026-07-08 accepted (phase 8a) — Retention is a hybrid: whole-partition drop plus a bounded sweep of survivors
- 0223 2026-07-08 accepted (phase 8a) — Retention respects a MIN(committed) drop floor unless explicitly opted out
- 0224 2026-07-08 superseded (phase 8a) — Partition labs swap message_log to a lab-scale width and restore the schema on exit
- 0225 2026-07-08 accepted (phase 8a) — Register creates cursor rows only for CURSOR-type groups
- 0226 2026-07-08 superseded (phase 8a) — Retention shipped scoped to the one shared log, per-topic scoping deferred
- 0227 2026-07-08 accepted (phase 8a) — Partition automation is the Janitor loop in Go, not pg_partman
- 0241 2026-07-10 accepted (phase 8b) — Each topic is its own physical table with its own sequence, partitions, and janitor
- 0242 2026-07-10 accepted (phase 8b) — routing_key/bindings stay a coarser concept above topics, not folded into them
- 0243 2026-07-10 accepted (phase 8b) — topic_id joins the keys of cursor/deliveries/binding; lease gets the column only
- 0244 2026-07-10 accepted (phase 8b) — Topic identity is a per-call datastore parameter, not a field on the datastore
- 0245 2026-07-10 accepted (phase 8b) — Topics are registered explicitly and never auto-created on use
- 0246 2026-07-10 accepted (phase 8b) — deliveries stays one shared table across every topic, unlike message_log
- 0247 2026-07-10 accepted (phase 8b) — No deliveries.status index until real evidence demands one
- 0248 2026-07-10 accepted (phase 8b) — Two routing_key slices sharing one topic share that topic's drop floor, by design
- 0261 2026-07-11 accepted (phase 8c) — Compacted topics resolve latest-per-key at claim time, not by background deletion
- 0262 2026-07-11 accepted (phase 8c) — latest_key trades a second synchronous write per keyed publish for an O(1) latest-per-key read
- 0263 2026-07-11 accepted (phase 8c) — The latest-per-key predicate is unbounded, not bounded by the claim's own high
- 0264 2026-07-11 superseded (phase 8c) — The compaction_key index is partial, covering only keyed rows
- 0265 2026-07-11 accepted (phase 8c) — Optional produce inputs travel in a ProduceOptions struct, not positional strings
- 0266 2026-07-11 accepted (phase 8c) — Key deletion is expressed in the payload; there is no schema-level tombstone
- 0267 2026-07-11 accepted (phase 8c) — The latest_key upsert guard compares id values, not commit order
- 0268 2026-07-11 accepted (phase 8c) — latest_key is one shared table across all topics, not per-topic
- 0269 2026-07-11 accepted (phase 8c) — Retention stays compaction-unaware; janitors garbage-collect dangling latest_key rows
- 0270 2026-07-11 rejected (phase 8c) — A latest_key backfill was built, verified live, then reverted
- 0271 2026-07-11 accepted (phase 8c) — The correlated scan's cost was measured before committing to latest_key
- 0272 2026-07-11 accepted (phase 8c) — The compaction_key partial index was dropped once latest_key left it without a consumer
- 0273 2026-07-11 accepted (phase 8c) — The synchronous latest_key write cost was measured and accepted, hot-key serialization included
- 0281 2026-07-12 accepted (phase 9) — Every consumerFunc failure shape lands in the existing per-message retry/backoff/dead-letter path
- 0282 2026-07-12 accepted (phase 9) — Datastore blips are retried via pkg/retry with explicit retryable/permanent classification
- 0283 2026-07-12 accepted (phase 9) — An idempotency_key claim table prevents double-publish on ambiguous commit acks
- 0284 2026-07-12 accepted (phase 9) — The idempotency check's measured cost was cut with a batched CTE and a per-call opt-out
- 0285 2026-07-12 accepted (phase 9) — Commit() classification lives inline at each call site; Wrap passes pre-classified errors through
- 0286 2026-07-12 accepted (phase 9) — Graceful shutdown narrows the interrupted lease instead of freeing it
- 0287 2026-07-12 accepted (phase 9) — One shared callSafely wraps consumerFunc for all three claim paths
- 0288 2026-07-12 accepted (phase 9) — The hard per-message timeout is a detached-goroutine race, with abandonment accepted and tracked
- 0289 2026-07-12 accepted (phase 9) — WorkTimeoutGrace defaults to 100ms, sized from a measured scheduler latency
- 0290 2026-07-12 accepted (phase 9) — Timeout errors record the message id in last_error, never the work payload
- 0291 2026-07-12 accepted (phase 9) — Panic recovery's defer lives inside the spawned goroutine and sends into done
- 0292 2026-07-12 accepted (phase 9) — AbandonedRoutines.Remove runs after recover() within the same defer
- 0293 2026-07-12 accepted (phase 9) — The abandoned-goroutine registry is a mutex-guarded map keyed by (MessageId, Attempt), kept as plain in-process state
- 0294 2026-07-12 accepted (phase 9) — The retry.Wrap success-after-retries bug was fixed with a plain early-return, not a classification rewrite
- 0301 2026-07-14 accepted (phase 10) — The waterline rollup stays lazy; no cursor update at commit time
- 0302 2026-07-14 accepted (phase 10) — pkg/ depends on the OTel metric API only, never the SDK or an exporter
- 0303 2026-07-14 accepted (phase 10) — The metrics snapshot is the single source for both the debug readout and the OTel instruments
- 0304 2026-07-14 accepted (phase 10) — Logging goes through a caller-supplied interface with an io.Writer-backed default
- 0305 2026-07-14 accepted (phase 10) — One queue-state query is the only derivation of the DB-truth health numbers
- 0321 2026-07-16 accepted (phase 11) — Multi-topic atomic publish exposes the transaction via a closure, not a declarative PublishAll
- 0322 2026-07-16 accepted (phase 11) — Partition self-heal in ProduceInTx is per-target via SAVEPOINT, with no opt-out
- 0323 2026-07-16 accepted (phase 11) — InTransaction does not retry
- 0324 2026-07-16 accepted (phase 11) — delivery_log_<topic_id> records only failed attempts; a row's absence is the success signal
- 0325 2026-07-16 accepted (phase 11) — deliveries went per-topic, and the six remaining shared tables were singularized, before the audit table shipped
- 0326 2026-07-16 accepted (phase 11) — Nested timeouts use context.WithTimeoutCause naming the budget that fired
- 0327 2026-07-16 accepted (phase 11) — Commit's per-exception writes are batched; RecordException stays one message at a time
- 0328 2026-07-16 accepted (phase 11) — The Datastore interfaces' fate was deferred to a standing cleanup phase, not decided mid-audit
- 0341 2026-07-20 accepted (phase 11.5) — Schema lives as versioned Go code; golang-migrate and its .sql files are deleted
- 0342 2026-07-20 accepted (phase 11.5) — A migration is a sparse struct of func fields, run against a Querier that cannot manage transactions
- 0343 2026-07-20 accepted (phase 11.5) — One append-only schema_log table; current version is the latest-by-id success row, not MAX(schema_version)
- 0344 2026-07-20 accepted (phase 11.5) — System scope and topic scope version independently
- 0345 2026-07-20 accepted (phase 11.5) — One advisory lock id, two hold-times: session-scoped for a migrate run, xact-scoped for RegisterSystem
- 0346 2026-07-20 accepted (phase 11.5) — Migration registries are explicit ordered slices, not init() registration
- 0347 2026-07-20 accepted (phase 11.5) — Missing or mismatched schema surfaces as teaching errors, not raw Postgres errors
- 0348 2026-07-20 superseded (phase 11.5) — AlterTopic takes a sparse pointer-per-field patch; nil means leave alone
- 0349 2026-07-20 accepted (phase 11.5) — The alter UPDATE is one static COALESCE($n, col) statement, not a dynamically built SET list
- 0350 2026-07-20 accepted (phase 11.5) — PartitionSize is immutable by omission from AlterConfig; dynamic partition bounds deferred
- 0351 2026-07-20 accepted (phase 11.5) — renameTopic pins the id first and updates WHERE id = $1, never WHERE name = $1
- 0352 2026-07-20 accepted (phase 11.5) — Renaming a topic is its own verb, not a NewName field on the alter patch
- 0353 2026-07-20 accepted (phase 11.5) — delivery_log_<id> is always created; DisableDeliveryLog gates only the writes
- 0354 2026-07-20 accepted (phase 11.5) — The CLI is a nested Go module, resolved locally through a gitignored go.work
- 0355 2026-07-20 accepted (phase 11.5) — CLI flags map onto the sparse config structs via cmd.Flags().Changed
- 0356 2026-07-20 accepted (phase 11.5) — No git tag marks this body of work
- 0361 2026-07-28 superseded (phase 13) — MessageProducer.Register(ctx) captures the instance lifetime and gates Produce
- 0362 2026-07-28 accepted (phase 13) — NewMessageProducer takes the raw PostgresDatastore and builds its datastores internally
- 0363 2026-07-28 accepted (phase 13) — Register validates the topic handle fail-fast: by-name fetch plus whole-struct compare
- 0364 2026-07-28 accepted (phase 13) — A non-cancellable lifecycle ctx is an error, with an explicit DisableGracefulShutdown opt-out
- 0365 2026-07-28 accepted (phase 13) — Registration is once per instance, and lifecycle sentinels live in pkg/errors
- 0366 2026-07-28 accepted (phase 13) — Consumer session loops keep their ctx and run under a merged ctx; wind-down returns nil
- 0367 2026-07-28 accepted (phase 13) — Janitor splits into a gated public entrypoint and a private loop
- 0368 2026-07-28 accepted (phase 13) — The consumer shutdown hook is deleted; the app that constructs the datastore closes it
- 0369 2026-07-28 accepted (phase 13) — The payload generic is named Message, cascading Work* type names to Message*
- 0370 2026-07-28 accepted (phase 13) — Topic administration moves to an admin object holding the datastore
- 0371 2026-07-28 accepted (phase 13) — The table-name functions move to internal/topic
- 0372 2026-07-28 accepted (phase 13) — Janitor and partition tuning fields are topic-scoped and persisted on the topic row
- 0373 2026-07-28 accepted (phase 13) — QueueTimeout is renamed QueueMargin; WorkTimeout and AckMargin keep their names
- 0374 2026-07-28 accepted (phase 13) — Datastore retry policy becomes retry.Policy, carried per config, not a global
- 0375 2026-07-28 accepted (phase 13) — Exception retry backoff reuses retry.Policy instead of a separate config surface
- 0376 2026-07-28 superseded (phase 13) — Always-on idempotency: make idempotency_key the enforced identity of message_log
- 0377 2026-07-28 accepted (phase 13) — Configs validate at construction via an exported WithDefaults-then-Validate pair
- 0378 2026-07-28 accepted (phase 13) — PartitionSafetyBuffer is deleted; the janitor always keeps the next partition created
- 0379 2026-07-28 rejected (phase 13) — Multi-partition create-ahead (a PartitionsAhead knob) is rejected; PartitionSize is the lever
- 0380 2026-07-28 accepted (phase 13) — The datastore interfaces are deleted in favor of the concrete types
- 0381 2026-08-01 accepted (phase 14) — topic.Destroy drops partitions in batches across multiple transactions
- 0382 2026-08-01 accepted (phase 14) — Partition batches use plain DROP TABLE IF EXISTS with no DETACH first
- 0383 2026-08-01 accepted (phase 14) — Partition-drop batch size is a fixed constant of 100, not a config knob
- 0384 2026-08-01 accepted (phase 14) — The partition drain loop is bounded, giving up via an unexported sentinel
- 0385 2026-08-01 accepted (phase 14) — The abandoned-routines tracking map stays unbounded; no config knob
- 0386 2026-08-01 accepted (phase 14) — The abandoned-routine Add/Remove race is fixed structurally with a reaper goroutine
- 0387 2026-08-01 accepted (phase 14) — A missing cursor row errors loudly, detected by restructuring the claim query instead of adding an existence check
- 0388 2026-08-01 accepted (phase 14) — The cursor-claim query locks the cursor row with FOR UPDATE in the old_values read
- 0389 2026-08-01 accepted (phase 14) — FanOut tracks a per-group high-water mark on the cursor table; LIFECYCLE groups register cursor rows and pin retention
- 0390 2026-08-01 accepted (phase 14) — FanOut delivers eagerly past the proven head; only the mark waits for proof
- 0391 2026-08-01 accepted (phase 14) — FanOut's scan bound is a scalar subquery, not a join on old_values
- 0392 2026-08-01 accepted (phase 14) — cursor claimed advances via GREATEST so a group running both paths cannot regress its frontier
- 0393 2026-08-01 accepted (phase 14) — Binding changes are forward-only; history the FanOut mark has passed stays as routed
- 0394 2026-08-01 accepted (phase 14) — Cursor claims stop at a proven head via a snapshot fence, not the visible MAX(id)
- 0395 2026-08-01 accepted (phase 14) — The pending (head, xmax) pair is stored unconditionally on every poll
- 0396 2026-08-01 accepted (phase 14) — An idle poll short-circuits after the read-only snapshot statement
- 0397 2026-08-01 accepted (phase 14) — No index on deliveries status for v1; reopen only on measured evidence, and prefer a partial index then
- 0398 2026-08-01 accepted (phase 14) — No DELETE CASCADEs and no triggers; integrity and logging stay in visible DML
- 0399 2026-08-01 accepted (phase 14) — Process keeps per-tick-fatal claim errors; no loop-level retry/backoff
- 0400 2026-08-01 accepted (phase 14) — Connection-death SQLSTATEs are retryable; resource-exhaustion, corruption, and misconfiguration codes are not
- 0401 2026-08-06 accepted (phase 14a) — A message schema change is a new physical topic under the same name, never an in-place migration
- 0402 2026-08-06 accepted (phase 14a) — Migrating a compacted topic to a new version is a user-space bridge consumer, not a library verb
- 0403 2026-08-06 accepted (phase 14a) — Compaction's winner rule generalized to a signed caller-supplied rank compared before id
- 0404 2026-08-06 accepted (phase 14a) — Message row metadata reaches consumer functions via consumer.MetaFromContext, not a signature change
- 0405 2026-08-06 accepted (phase 14a) — The topic catalog is keyed (name, schema_version), and constructors require SchemaVersion positionally
- 0406 2026-08-06 accepted (phase 14a) — FamilyHealth never reports a compacted topic safe to retire
- 0407 2026-08-06 accepted (phase 14a) — The bridge derives its IdempotencyKey deterministically from the source message id
- 0408 2026-08-06 accepted (phase 14a) — The topic catalog column is named schema_version, accepting the overlap with schema_log.schema_version
- 0409 2026-08-06 accepted (phase 14a) — There is no unversioned GetTopic(name); every topic read is version-addressed
- 0410 2026-08-06 accepted (phase 14a) — Topic display identity stays two structured fields, not a concatenated name@vN string
- 0411 2026-08-06 accepted (phase 14a) — FamilyHealth and VersionHealth live in pkg/admin, not as a pkg/metrics composition
- 0421 2026-08-08 accepted (phase 14a) — One generic worker/worker_instance pair replaces per-feature maintenance/duty plumbing
- 0422 2026-08-08 accepted (phase 14a) — Exclusivity is claim-per-instance, not claim-per-tick
- 0423 2026-08-08 accepted (phase 14a) — The manager respawns only on ErrInstanceLost and propagates every other error
- 0424 2026-08-08 accepted (phase 14a) — The consumer inversion: consumption loops are worker rows the manager spawns
- 0425 2026-08-09 accepted (phase 14a) — One system manager row; the daemon is the same manager at deployment scope
- 0426 2026-08-08 accepted (phase 14a) — Producer split into pure factory plus Register; the stored lifecycleCtx dropped
- 0427 2026-08-08 accepted (phase 14a) — Snapshot verdicts are classify functions returning named enums
- 0428 2026-08-11 accepted (phase 14a) — EnsureNextPartition deleted without a replacement home
- 0429 2026-08-08 accepted (phase 14a) — Duplicate beside pkg/maintain, don't retrofit it
- 0430 2026-08-08 accepted (phase 14a) — NoInstanceTarget (-1) for self-claimed consumer loops
- 0431 2026-08-09 accepted (phase 14a) — The umbrella package is systemmanager, not Maintainer
- 0441 2026-08-02 accepted (phase 14a) — Every domain gets the same three-layer package shape
- 0442 2026-08-02 accepted (phase 14a) — All input validation lives at the controller; datastores trust their inputs
- 0443 2026-08-04 accepted (phase 14a) — pkg/consumer is a door, not a vocabulary layer
- 0444 2026-08-04 accepted (phase 14a) — Consumption-loop packages sit under the door with their own narrow configs
- 0445 2026-08-04 accepted (phase 14a) — MessageMeta lives in the one new leaf, pkg/consumer/message
- 0446 2026-08-04 accepted (phase 14a) — ConsumerDatastore's phantom type parameter deleted
- 0447 2026-08-04 accepted (phase 14a) — uuid.UUID above the datastore, pgtype.UUID below; ErrLeaseLost declared where detected
- 0448 2026-08-04 superseded (phase 14a) — No shared base package for the consumption-loop packages at first
- 0449 2026-08-04 accepted (phase 14a) — deliveryconsumer kept in the tree but not wired to run
- 0450 2026-08-04 accepted (phase 14a) — rangeState and claimBuffer stay private, not their own packages
- 0451 2026-08-04 accepted (phase 14a) — Consumption-loop package names keep the house stutter for now
- 0461 2026-08-12 accepted (phase 14a) — Scheduled work is built on the existing messaging machinery, with one compacted `__system.job_requests` topic
- 0462 2026-08-12 accepted (phase 14a) — The job name is the routing key; there is no handler column
- 0463 2026-08-12 accepted (phase 14a) — Cron concurrency is enforced at consume time by the key lease, never by the scheduler
- 0464 2026-08-12 accepted (phase 14a) — Missed scheduled times are dropped: the scheduler walks to the newest due time and produces only that
- 0465 2026-08-12 accepted (phase 14a) — Job-request status is derived from existing tables, never written
- 0466 2026-08-12 accepted (phase 14a) — `__system.job_requests` runs with `DeliveryLogModeAll` so successes leave rows
- 0467 2026-08-12 accepted (phase 14a) — Status classification checks terminal outcomes before the not-head "superseded" case
- 0468 2026-08-12 accepted (phase 14a) — Registry verbs: idempotent `RegisterCronJob` that errors on config mismatch, `Alter` re-seeds `next_scheduled_time`, destroy gated by `AllowDestroy`
- 0469 2026-08-12 accepted (phase 14a) — Vendor the robfig cron schedule core instead of hand-rolling or adding a dependency
- 0470 2026-08-12 accepted (phase 14a) — The scheduler produces each due job in its own transaction
- 0471 2026-08-12 accepted (phase 14a) — `RunCronJob` takes a fresh v7 idempotency key per call and defaults to concurrency 'allow'
- 0472 2026-08-12 accepted (phase 14a) — "Firing" is retired from the codebase's vocabulary
- 0473 2026-08-12 accepted (phase 14a) — Status and request-listing reads are flat per-fact queries composed in Go, not one CTE statement
- 0481 2026-08-13 accepted (phase 14a) — Each default alert check is its own cron job and worker-kind subpackage
- 0482 2026-08-13 accepted (phase 14a) — One consumer group per check; the central alert dispatcher is dead
- 0483 2026-08-13 accepted (phase 14a) — The `__system.alerts` topic is the alert state store, dedup memory, and integration surface
- 0484 2026-08-13 accepted (phase 14a) — Alert transitions are decided by one pure function, and evidence never enters it
- 0485 2026-08-13 accepted (phase 14a) — There is no notifier component: notification is a side effect of `Record` observing a status change
- 0486 2026-08-13 accepted (phase 14a) — Alert executors are worker definitions the manager claims, never embedded goroutines
- 0487 2026-08-13 accepted (phase 14a) — The alert domain owns measurement and decision; producer and consumer inject it
- 0488 2026-08-13 accepted (phase 14a) — "handler" and "publisher" are retired as domain nouns; the write door is `AlertController.Record`
- 0489 2026-08-13 accepted (phase 14a) — Checks declare themselves at `RegisterSystem`, with binds made idempotent by `UNIQUE` + `ON CONFLICT DO NOTHING`
- 0490 2026-08-13 accepted (phase 14a) — The register-time evaluator pass is log-only and never writes to the alerts topic
- 0501 2026-08-01 accepted (phase 13) — Schema provisioning is a library call, not an external migrate step
- 0502 2026-07-28 accepted (phase 13) — The consumer gets an opt-in circuit breaker for systemic downstream failure
- 0503 2026-07-28 accepted (phase 13) — Breaker input is user error classification, recorded as error_class on the delivery row
- 0504 2026-07-28 accepted (phase 13) — The breaker trips per instance; global OPEN is a quorum of locally-open instances
- 0505 2026-07-28 accepted (phase 13) — The breaker's trip threshold is a conservative debounce, not statistics
- 0506 2026-07-28 accepted (phase 13) — Reconciliation refunds systemic failures on CLOSE, run by the probe winner; recovery is automatic
- 0507 2026-08-01 accepted (phase 13) — The public surface is organized by three audiences; plumbing types are demoted
- 0508 2026-08-01 accepted (phase 13) — The concurrency package goes internal; consumers build queue and pool from ConsumerConfig
- 0509 2026-08-01 accepted (phase 13) — The retry surface is trimmed to Policy and the two error types
- 0510 2026-08-01 accepted (phase 13) — ConsumerType and its constants are demoted; NewConsumer defaults to cursor consumption
- 0511 2026-08-13 accepted (phase 14a) — Binding sets are declared at consumer Register; replacement waits for zero live declarers
- 0512 2026-08-14 accepted (phase 14) — Producer create-ahead triggers at the partition's 80% mark id; heal path serialized by advisory lock
- 0513 2026-08-14 accepted (phase 14) — Create-ahead ships with the 95% backstop mark, and destroyed topics evict their claim entry (amends 0512)
- 0514 2026-08-15 accepted (phase 14) — DestroySystem is RegisterSystem's inverse: drop every topic, then the control-plane schema
- 0515 2026-08-15 superseded (phase 14) — Group tunables live in worker metadata as {default, override}; users tune through domain nouns
- 0516 2026-08-15 accepted (phase 14) — AlertRepeatInterval moves to alert worker metadata; system config becomes a stub
- 0517 2026-08-15 superseded (phase 14) — Uniform config get/set/unset surface; alter dies in the CLI; Go Alter* verbs take tri-state Update[T] (amends 0515, 0516)
- 0518 2026-08-15 accepted (phase 14) — Config is code-owned, the latest declaration wins, and the CLI never writes config (supersedes 0515, 0517)
- 0519 2026-08-15 superseded by 0570 (phase 14) — Topic config lives in append-only declaration rows; topic keeps identity only
- 0520 2026-08-15 accepted (phase 14) — Cron jobs are declared like every other resource; built-in alert config moves into RegisterSystem (amends 0518)
- 0521 2026-08-15 accepted (phase 14) — Only a declarer writes config, so the CLI creates nothing (amends 0518, 0520)
- 0522 2026-08-16 accepted (phase 14) — Metrics are samples on __system.metrics written by a collector worker; otel exposure moves to its own module
- 0523 2026-08-16 accepted (phase 14) — The metric point type is Measurement, not Sample (amends 0522)
- 0524 2026-08-16 accepted (phase 14) — Alert pipeline instrumentation: state gauges pulled by the collector, run outcomes pushed by the handler (follows 0522)
- 0525 2026-08-16 accepted (phase 14) — ProduceBatch: one call, N messages, one transaction (explicit batch verb beside the batcher)
- 0526 2026-08-17 accepted (phase 14b) — migrate.Controller is the package's only door; the schema gate reads by id; Owner names are never empty
- 0527 2026-08-17 accepted (phase 14b) — pkg/migrate adopts the three-layer template (vocabulary root, controller, controller/datastore; ToStep becomes a controller adapter, ErrNotRegistered rehomed to vocabulary)
- 0528 2026-08-17 accepted (phase 14b) — pkg/logger, pkg/retry, pkg/errors, pkg/context merge into a flat pkg/common (RetryPolicy/RetryDatastore/LoggerWith renames; concurrency excluded, destined internal/)
- 0529 2026-08-17 accepted (phase 14b) — one Querier contract (Exec/Query/QueryRow/SendBatch/CopyFrom); producer Tx embeds it; the produce transaction is the one sanctioned package crossing, cronscheduler a user of the public seam
- 0530 2026-08-18 accepted (phase 14b) — pkg/compaction stays two-layer (owns no vocabulary); MessageRow stays in common as cross-stack shared vocabulary
- 0531 2026-08-18 accepted (phase 14b) — system-topic and cron-job declarations live in the domain's controller (TopicConfig to system_topic.go; alert Job/NewJob/ToJobData to alert/controller; TopicName consts stay vocabulary)
- 0532 2026-08-18 accepted (phase 14b) — consumer read-models live with the controller whose verbs return them; vocabulary homes are earned by cross-domain consumers (binding.Declaration the model); cursor.go split, typed DeliveryStatus
- 0533 2026-08-18 accepted (phase 14b) — field absence is the zero value (`// "" if unset`), never a nil pointer; tri-state scalars widen the domain or get a named state; whole-entity absence stays the (nil, nil) Get
- 0534 2026-08-18 accepted (phase 14b) — metrics write door = pkg/metrics/producer: MetricsProducer ∥ MetricsController domain-layer naming, GoRoutineEvent → metrics vocabulary, consumer/metrics deleted; config gains WithDefaults/Validate, nil-safe receiver/Noop/ctx params deleted
- 0535 2026-08-18 accepted (phase 14b) — consumer/base cleanup: GetTopic resolves for a pure NewBaseConsumer, BaseConsumerConfig/BaseDefinitionConfig, ReleaseKeyedRun symmetry (KeyLeases unexported), AckMargin→RecordMargin, keylease token minted in controller + migrate runStep normalized (AppendMessageBatch the one sanctioned non-Wrap public)
- 0536 2026-08-18 accepted (phase 14b) — MessageOptions stays the sanctioned nilable sparse sub-document (never-nil COALESCE/NULLIF reshape built then rejected by user; narrows 0533's consequence; nil-tolerant Fill/Clamp are the resolution boundary)
- 0537 2026-08-18 accepted (phase 14b) — every worker kind carries a controller (user-settled B): janitor + waterline grew controller layers over their datastores, executions call them; cronscheduler rides the produce-seam chunk; AdvanceWaterline return kept (labs assert on it), two-statement advance reconfirmed + stated
- 0538 2026-08-19 accepted (phase 14b) — file content ordering convention (CONVENTIONS.md File layout): free vars/consts top, type block (struct/New/validates), pair-by-pair or lifecycle order, helper = free non-receiver func in a method file, all helpers bottom behind the HELPERS banner; sweep batched with blank-line convention
- 0539 2026-08-19 accepted (phase 14b) — blank-line convention (CONVENTIONS.md Blank lines): bodies read as paragraphs, one blank between steps (a step = what one comment could name), glue rules for err checks/comma-ok/defer/SQL exec, comment binds downward, validation preamble one step, max one blank; full paragraph rule enforced (option A), sweep batched with 0538
- 0540 2026-08-19 accepted (phase 14b) — bare sub-consumer constructors fenced by docs, not structure: package docs state one-worker-row-not-assembled-group + what running alone lacks, constructors point to consumer.NewConsumer; no signature change (trim keeps them public for custom fleets; later passes re-touch signatures)
- 0541 2026-08-19 accepted (phase 14b) — examples/bench/reference became dev-only nested modules (own go.mod, go.work use, no parent require, never tagged): keeps labs + scratch out of the published module zip and root `go test ./...`; root go.mod was already minimal (tidy no-op), so the go.mod-cleanup follow-up dissolved
- 0542 2026-08-19 accepted (phase 14b) — config & options shape decisions: Config keeps its name + CONVENTIONS.md rule (Config = only optional fields; required values become constructor params — PostgresConnectionConfig's User/Host/Database moved into NewPostgresDatastore); ProduceOptions compaction nested as Compaction *CompactionOptions{Key,Rank} via NewCompactionOptions (nil = not compacted, rank-without-key unrepresentable); Consumer.Register keeps 5 params, NewConsumerInstance unexported
- 0543 2026-08-19 accepted (phase 14b) — config field order standardized domain-first with ambient tail (Logger, Retry, per-loop curves; CONVENTIONS.md rule): six drifted configs reordered incl. ConsumerConfig; cron_job.suspended + delivery outcome-state/lease DDL columns regrouped by likeness (drop+recreate verified); lease `RETURNING *` (freshclaim/reclaim) fixed to named columns — the SELECT * ban covers RETURNING
- 0544 2026-08-19 accepted (phase 14b) — dead-field pass: WorkerSnapshot.OldestInstanceAge chain deleted (metrics collector derives oldest-unclaimed from UnclaimedFor; worker_instance.created_at stays as audit bookkeeping), WorkerInstanceData.ExpiresAt deleted (scanned, never adapted); JobRequest.CronJobId exempted — wire-payload contract fields are judged by the wire contract, not the read-model rule; staticcheck+unparam clean across all three code modules
- 0545 2026-08-19 accepted (phase 14b) — waterline renamed cursor_advancer end to end (pkg/worker/cursoradvancer, worker.name value, labs, CLI): AdvanceWaterline → AdvanceCommitted, RollRetry → AdvanceRetry; comments describe cursor.committed directly; reference/, bench/, and doc history keep the old word
- 0546 2026-08-19 accepted (phase 14b) — controller + datastore verbs drop their own domain noun where it is the type's single noun and nothing collides (Topic, CronJob, System, Compaction, KeyLease, CronScheduler, ExceptionConsumer — now symmetric with DeliveryConsumer); Worker/Consumer/Metrics/migrate/Janitor/Producer controllers and the MessageAdmin facade keep their nouns, each for a stated reason
- 0547 2026-08-19 accepted (phase 14b) — run-side worker structs renamed *Instance matching the worker_instance row they hold (execution.go → instance.go, manager pool follows); worker.Execution survives only as the interface; no concrete Definition/Provisioner split — a data-only definition has no consumer, the DB worker row is the real definition
- 0548 2026-08-19 accepted (phase 14b) — receiver letter codified as the initial of the type's FINAL word (CONVENTIONS.md); swept: every *Definition → d, *Instance → i, MetricsProducer → p, result → r
- 0549 2026-08-19 accepted (phase 14b) — narrows [0547]: worker.Definition becomes a data struct (Name/Metadata/OwnerKind/TargetInstances) built and stored by each kind's constructor; concrete machines renamed *Provisioner; Provisioner = Definition() + Provision(declared *worker.Worker); one WorkerController.DeclareWorker verb ends every Declare (Declarer stays universal, alerts keep their group/binding preamble); Declare returns only error — provisioning always re-reads the row
- 0550 2026-08-19 accepted (pre-v1) — structured error anatomy: common.Error carries code (VK+4 digits, flat serial, append-only like decision records) + problem + values (named pairs) + fix + derived docs URL + recovery (Transient|Permanent, test-enforced tense); Err* variables declare via common.NewError, raise sites attach values only, errors.Is = code identity, one renderer per surface; CONVENTIONS.md ## Errors written to 2026 agent-authoring evidence; "sentinel" retired for "named error variable"; rejected: class-grouped codes, interpolated-prose values, docs URL in the one-liner
- 0551 2026-08-19 accepted (pre-v1) — retry classification is consulted, never encoded: RetryDatastore is the ONE retry type (plain Retry merged away, retry_error.go's RetryableError/PermanentError/IsRetryable deleted); Wrap consults IsTransientDatastoreError (recovery on the error first, then IsTransientPgError); per-call overrides became declared errors raised with .Wrap(cause) (errPartitionLockTimeout VK0018, ErrCommitConfirmationLost VK0019); rejected: classifier func field defaulted-then-overwritten, classifier as constructor param; general-purpose retry deferred to v1 API review
- 0552 2026-08-19 accepted (pre-v1) — a missing __system.* topic raises migrate.ErrNotRegistered (fix: RegisterSystem), never topic.ErrTopicNotFound whose declared fix (RegisterTopic) is wrong for reserved names; one mechanism for the fact across admin metrics/alert/cron, otelvulkan, alert provisioners; user topics keep ErrTopicNotFound
- 0553 2026-08-19 accepted (pre-v1) — the code boundary: a condition earns a declared error by cross-package errors.Is brancher, OR recovery differing from IsTransientDatastoreError's own conclusion, OR user-facing docs-page worth; validation/invariant guards/same-package signals stay plain on the templates; full 618-site audit promoted 5 (VK0020 partitions-remain, VK0021/24 declaration-interrupted races now Transient-healed, VK0022/23 schema version skew) and folded two topic-not-registered prose duplicates into ErrTopicNotFound
- 0554 2026-08-20 accepted (pre-v1) — plain-error construction standard: CONVENTIONS.md "When writing a plain error" (templates/bans/tense apply below the boundary; constraint guards end ", got <value>"; names spelled as the caller knows them; errors.New for static text; fix clauses allowed, cross-package fix = promotion tell; wrap only the owned fact); swept project-wide (33 got-clauses, 6 static fmt.Errorf, rewordings, VK0004 raise moved to With); promoted the missed third declaration race cron.ErrDeclarationInterrupted VK0025; open in TODO: binding-declaration datastore cannot raise ErrGroupNotFound (declared in controller, above the datastore)
- 0555 2026-08-20 accepted (pre-v1) — package kinds + seam law: three kinds (infrastructure / domain: vocab root <- controller <- datastore + its workers / API package: producer, consumer, admin, systemmanager — no errors, no SQL, no vocab); seam law (what another stack imports is vocab or a domain controller; own-tree-only nests freely — producer keeps its controller); placement law (a worker lives under the domain whose tables it maintains); NEW pkg/consumergroup (VK0014-16 + binding + MessageMeta + subconsumers + cursoradvancer), janitor->topic, cronscheduler->cron/scheduler, metricscollector->metrics/collector, VK0008/0009 admin->topic, migrate root stays (Querier = infrastructure seam); "door" banned; rejected: pkg/message domain, vocab leaf under consumer
- 0556 2026-08-20 accepted (pre-v1) — standing walks over plain raise strings: internal/errorregistry plain_errors_test.go, ast-based (errors.New/fmt.Errorf string literals over pkg/+otelvulkan/+cmd/vulkan, robfig skipped); four mechanical rules (banned words shared regexp, static-fmt.Errorf, constraint got-clause with nil/zero exclusion, declared-problem restatement); tense/naming/fix stay review-time; exceptions = explicit test entries, no code markers
- 0557 2026-08-20 accepted (pre-v1) — developer tooling = dev-only tools/ module (6th go.work entry, never tagged, no parent require): internal/errorregistry -> tools/conventions (one package named for the document it enforces; registry shim + all machine-checkable CONVENTIONS.md walks; convenience deps stay out of the root graph); enforcement = `just verify` (root build/vet/test + cmd/otelvulkan/examples/bench builds + tools walks), the future CI command; rejected: hack/ name (k8s jargon), internal/conventions (module boundary beats package naming), go/analysis analyzers until a rule needs types/IDE
- 0558 2026-08-20 accepted (pre-v1) — logging rule sheet (CONVENTIONS.md ## Logging): logs+errors one message system; levels by "who must act" (tick streak past curve cap = the Warn->Error escalation), silent steady state, static lowercase messages under problem-line rules, attr key registry (error as value, topic/topic_id split, <verb>_count), identity bound once via LoggerWith, start line = diagnosis snapshot (vulkan_version + config facts), default logger stdout->stderr, `vulkan explain <code>`; 108-site sweep; rejected: OTel dotted keys, DiscardHandler default
- 0559 2026-08-20 accepted (pre-v1) — per-operation debug buffer at the Logger seam: WithLogBuffer(ctx) opens a 64-record ring, BufferLogger (idempotent, LoggerWith hoists it outermost) holds Debug/Info/Warn and drains into the first Error's `preceding` group attr; boundaries = Produce/ProduceBatch, CallSafely, worker tick; rejected: slog.Handler-level replay, Warn as drain trigger
- 0560 2026-08-20 accepted (pre-v1) — SQL literal owner comments: every literal's first line `-- vulkan: <package>.<method>` (domain segment, not "datastore"); pg_stat_statements/server log attribute load to library verbs, constant text so statement caching unaffected
- 0561 2026-08-20 accepted (pre-v1) — logging machinery moves to pkg/common/logging (package logging: Logger/LoggerWith/NewDefaultLogger/BufferLogger/WithLogBuffer/ring; toAttrs duplicated per side, never exported); narrows 0528 (errors stay flat vs stdlib shadow, retry pinned by MessageOptions↔Error cross-import); infrastructure kind = common + machinery subpackages; ~315 renames/130 files; concurrency still destined internal/, never common/
- 0562 2026-08-20 accepted (pre-v1) — log events carry VK codes: common.NewLogEvent(code, message, consequence) -> *LogEvent{Code, Message}, shared VK serial space, one generic registry.go (map[string]Declaration, GetCode/GetKind, listRegistered[D]; typed listers live with their types), 0553-mirror boundary (operator-actionable Warn/Error only), declarations in owning vocabulary logs.go, call sites log Message + "code" attr first, 12 events VK0026-37 + hand-written pages on /errors/, vulkan explain lists both, conventions walks extended; no Level field (call site owns level); consumer start line gains message_timeout/shutdown_timeout/batch_limit
- 0563 2026-08-20 accepted (pre-v1) — coded declarations move to pkg/common/diagnostic: registry.go/error.go/event.go, LogEvent→Event (NewEvent/Events()/KindEvent — the qualifier carries "log"), Kind = KindError|KindEvent, domains declare via diagnostic.NewError/NewEvent; narrows 0561 twice-over (common root = vocabulary only; subpackages = diagnostic + logging); "diagnostic" dodges the stdlib errors shadow that pinned errors flat in 0528/0561
- 0564 2026-08-20 accepted (pre-v1) — repeated Warn/Error suppression as the pipeline's suppressHandler stage ([0565]): instance-scoped state (key = level+message, Warn/Error only), first occurrence passes, repeats in a 1-minute package-const window drop and count, next same-key emit carries suppressed_count; no flush timer (tail count lost, FDB precedent); sits after capture / before drain so the ring stays complete and a suppressed Error keeps the held narration; renewal heartbeat stays a repeating Warn
- 0565 2026-08-20 accepted (pre-v1) — logging.NewPipelineLogger(sink, cfg) is the ONE wrapper: PipelineLoggerConfig declares the composition (Args/Buffer/Suppress), building over an existing pipeline merges instead of nesting (Args concat, flags OR, suppress node carries over with its window state — state on the handler only); internally each call is a record walked through a one-method handler chain, fixed order capture→enrich→suppress→drain→sink; LoggerWith/BufferLogger/SuppressLogger and the wrapper types deleted, ~70 sites declare their composition; narrows 0559 (wrapper mechanism replaced, boundaries/ring/drain semantics stand); research: slog handlers, zap sampler core, Logback turbo filters, K8s EventCorrelator — no system composes concerns on a leveled front-end
- 0566 2026-08-21 accepted (pre-v1) — slow-operation threshold logging ([0558] log_min_duration axis): one Warn past a duration threshold at the [0559] boundaries — every produce entry point (ProduceFunc/InTx include caller closure/txn time), CallSafely dispatch, worker tick; Warn not Debug (default logger is WARN-up, a Debug line leaves the knob dead; [0564] suppression storm-guards); timing = call-site time.Since, never a pipeline stage; logs on error paths too (slowness is its own fact); SlowProduceThreshold + SlowDispatchThreshold (0 = disabled), tick has no field — its threshold IS the row's poll_rate; events VK0038-40 (produce unexported in pkg/producer/logs.go, VK0033 precedent), attr rows duration/threshold added
- 0567 2026-08-21 accepted (pre-v1) — stop line as session summary: stopped line = bound identity + duration + lifetime counters (zeros printed), emitted on EVERY exit incl. fatal-error teardown, reads memory only never the DB; counters are metrics machinery — atomics + Snapshot() on the instance-side metrics producer (already threaded to message/exception provisioners, rename at build), bumps at worker layer from facts in hand, never in datastores; flush tick produces totals as KindCounter (first user; otelvulkan maps it already) with session-uuid series attr; counter set claimed/success/superseded/ready/deferred/dead/reclaimed/quarantined/abandoned/lease_lost; producer's produced counter alone waits on presence (13d); rejected: topic query at exit, per-bump produce, log-pipeline counting
- 0568 2026-08-21 accepted (pre-v1) — summary lines declare + help breadcrumb: [0562] boundary widened one notch (a lifecycle summary line whose attr set needs a docs page declares despite Info); stopped line = VK0041 unexported in pkg/consumer/logs.go, code first attr + trailing `help` attr ("metrics explained: vulkan explain VK0041", new registry row, summary lines only); VK0041 page IS the counter catalog (per counter: what it counts, what nonzero means, flow-vs-level warning); parked: pipeline-enrich auto-help on all coded lines; rejected: prose/URLs in the message, tooling-only (annotate mode unbuilt)
- 0569 2026-08-21 accepted (pre-v1) — metric declarations join the shared VK registry: diagnostic.Metric is the third declaration kind (NewMetric/Metrics()/KindMetric, one code registry, session counters VK0042-51); kind/unit plain strings held to the pkg/metrics vocabulary by a conventions walk; GetMetric = name scan of the one code map (no second name index; Measurement stays code-free, it is user-facing); explain resolves code/full name/stop-line attr key, ten hand-written pages; VK0052 abandoned-routine events dropped (failed batch + queue-cap drops, counted at enqueue, one flush-tick report); one MetricsProducer.Run tick loop (SessionFlushRate, 30s default); kept both read paths (ConsumerGroupSnapshot = live DB level, session series = flows, heads = cache for staleness-tolerant readers)
- 0570 2026-08-22 accepted (pre-v1) — topic truth stays on the topic row (UNIQUE constraint, plain reads, 23505 rename); append-only history is a display-only log written in the same txn, the binding [0511] shape; topic_log + binding_declaration→binding_log renames, parked failure-evidence table → worker_run_log (worker_log reserved for worker metadata history); supersedes 0519, rejects single-table stable-id (FK hole)
- 0571 2026-08-22 accepted (pre-v1) — per-topic table split rule: split when every row has one owning topic and no reader precedes topic context (cursor, lease, key_lease, compaction_head, binding+binding_log as one mechanism); shared = system-scope-capable rows (worker, worker_instance, cron_job, migration_log) + the catalog (system, topic, topic_log, consumer_group); topic family 3→9 tables, destroy = DROP TABLE, binding_log pruner amended to one DELETE per topic table
- 0572 2026-08-22 accepted (pre-v1) — append-only is for history tables: the [0511]/[0570] log-companion shape (mutable truth row + same-txn append-only log) is where append-only lives; coordination state (lease, key_lease, worker_instance, cursor), derived caches (compaction_head), and the dispatch index (delivery_<id>) stay update-in-place — their history is churn or already owned by a log table; blanket Cassandra-like append-only rejected (Cassandra is append-only at the storage engine, Postgres MVCC already is; data-model emulation = hand-built compaction minus HOT); parked orthogonal nugget: partition delivery_<id> + delivery_log_<id> BY RANGE (message_id) so partition-drop range DELETEs become drops
- 0573 2026-08-22 accepted (pre-v1) — binding_log retention: waiting rows past a flat 7d TTL swept in one batched DELETE, each declarer's newest waiting row kept (dead waiters stay listed), installed rows kept forever; new OwnerSystem worker consumer_group_janitor (hourly poll, Debug swept_count); janitor = the per-domain cleanup worker pattern, topic kind renamed topic_janitor; TTL const on the hardcoded-config audit; riding the cursor advancer rejected
- 0574 2026-08-22 accepted (pre-v1) — compaction-key deadlock evaluation: batched Produce provably cycle-free (batcher ascending-key sort; compactiondeadlocklab + every bench/compaction cell = zero deadlocks), ProduceInTx the one deadlock site with retry left to the caller (library 40P01/40001 closure-retry REJECTED for simplicity — never re-propose; guidance = order ProduceInTx calls by compaction key), hot-key cost a floor not a cliff (~50% of unkeyed throughput flat from cardinality ~64 to 1, ratio survives sync=off; hurt case = hot key x many producer processes, 6 batchers 2.4x worse with p99 >100ms); dead-tuple rate feeds the fillfactor audit
- 0575 2026-08-22 accepted (pre-v1) — public read-models carry json:"snake_case" struct tags: keys spell the log-attr registry name where one exists, otherwise the field name snake_cased; write shapes/configs/instances untagged (same boundary as the db: rule); audit clean — no pgtype in read-models, uuid.UUID marshals canonical; Duration fields marshal as ns ints, CLI renders durations as strings via [0576]
- 0576 2026-08-22 accepted (pre-v1) — CLI --output <text|json> root persistent flag: json stdout = exactly one parseable document per command (clig.dev), results are the natural document with [0575] keys, errors = {"error":{...}} LogValue mirror on stderr (plain errors reduced to problem only), failPrinted failures become result-document data, destroys emit {topic, version, topic_id, destroyed:true}-shaped records, cron run emits {cron_job, message_id}, alters echo the get-shape, migrate emits final summary only, manager run rejects the flag, -q+json = usage error, destroy+json requires --yes; rejected: streaming migrate events
- 0577 2026-08-22 accepted (pre-v1) — worker metadata history = append-only worker_log (completes [0570] reservation): full snapshot (name denormalized for join-free operator scans, metadata, target_instances, declared_by=ProcessIdentity, declared_at), create path appends in the INSERT's tx; replace path takes replaceConfig's decide-before-writing shape (server-side jsonb equality read, no-change redeclares write nothing — no log row, no dead tuple; the stored-alias UPDATE goes away), machinery never reads it, no retention (worker_log/topic_log TTL revisit parked)
- 0578 2026-08-22 accepted (pre-v1) — fillfactor audit: adopt nothing, defaults everywhere — cursor/compaction_head/delivery updates already ~100%/~99%/64-77% HOT at default and throughput identical within noise (delivery's apparent ff90 win = checkpoint variance, killed by 3x2 reps; ff20 cratered the 1024-key head cell); [0574]'s 36k dead tuples were HOT-chain tuples pruned in place; lease/key_lease insert->delete and worker_instance's indexed heartbeat ruled out structurally; bench/fillfactor = the consume-side harness, re-open only on a measured n_tup_hot_upd/n_tup_upd degradation
- 0579 2026-08-22 accepted (pre-v1) — migration txn steps run under lock_timeout: runStepWithTx sets SET LOCAL lock_timeout = 2s after Begin (own ddlLockTimeout const, no config field), 55P03 on the txn step path reclassified Transient via a new declared error (VK0018 precedent) so the rolled-back step retries under the existing bounded schedule; NoTxn steps keep fail-fast — no cap (CREATE INDEX CONCURRENTLY's INVALID-index hazard, session SET leaks to the pool); 2s kept: lock_timeout caps queue wait only and is the stall inflicted on queries queued behind the ALTER, patience lives in the retry schedule; build pre-v1, inert until release-era ALTER steps
- 0580 2026-08-22 accepted (pre-v1) — every migration step declares MinCompatibleVersion (0 = additive, own version = breaking; bool rejected — expand/contract's middle release needs a number; empty version-bump steps give schema-less releases a version to name); stored per migration_log row (v1.0 baseline DDL), gate admits min_compatible_version <= build <= current (build derived len(Registry)+1, Min/Max constants deleted; VK0023 attrs min_compatible_version + build_version); breaking releases stop old binaries first (gate is Register-only); tools/compat nested module + `just compat-lab` = the release-checkpoint empirical check that declared matches observed
- 0581 2026-08-22 accepted (pre-v1) — the doc site documents shipped behavior only: every non-error page rewritten to the real API with compile-checked samples, unshipped capabilities labeled Proposed (never a checkmark in comparison tables), no performance number without a benchmark record, ## Vocabulary governing prose incl. slugs (transactional-enqueue→transactional-produce, streams→fan-out); retired permanently: the cursor-vs-lifecycle choice pitch (ConsumerType) and Vulkan Cloud; the pass surfaced + fixed the RoutingKey doc comment and worker_log's missing DROP
- 0582 2026-08-23 accepted (pre-v1) — site stack: Starlight dropped (the board replaces everything it provides), Astro core kept — glob loader + own zod schema, Pagefind, @astrojs/mdx; glob generateId preserves URL casing so no redirects are owed; Svelte 5 the one island framework, no store library (`<name>-state.svelte.ts` runes modules), vanilla CUBE CSS with sibling component css, route-local `_<page>/` for page logic, client:load banned; website/CONVENTIONS.md + `just site-verify` (prettier/eslint/stylelint/astro check strictest/svelte-check/remark/Vale/vitest); open: spacing token scale, Playwright JS ceiling
- 0583 2026-08-23 accepted (pre-v1) — the doc site is a phpBB-era message board: page = thread, board = section, code page = a thread whose OP is the code and whose declared fix is the ACCEPTED ANSWER ([SOLVED] only when a fix exists); read state derives from ONE append-only localStorage page-visit log (cap 200, front pop) feeding the visit bar, amber folders and /whats-new/ alike; chrome speaks 2004 / content speaks now, amber means new-or-act only, pixel marks rectangle-native; Board.threads(ids) = the single membership source; real forum deferred
- 0584 2026-08-23 accepted (pre-v1) — the site's SQL console runs the library's own SQL in PGlite (Postgres wasm): every statement extracted byte-exact from the Go sources into its own file (found by the [0560] `-- vulkan:` tags), a vitest drift test asserting verbatim presence + count equality both ways, build-time shell rows produced by running the same statements in Node (a broken literal fails the build), status reporting only measured facts; deferred: try-it links, wasm idle prefetch, transition:persist, Go-wasm-over-PGlite bridge
- 0585 2026-08-24 accepted (pre-v1) — the claim path's snapshot gate proves on PGlite, so the sandbox's Tick can run the real freshClaimMessagesWithCursor unchanged: headless round trip (register group → produce → claim → read → commit) proves on the FIRST poll — snapshot pair (head=3, xmax=789), pg_snapshot_xmin 789, fresh-pair branch fires, cursor (0,3], settled_head 3; structural not lucky (the snapshot statement takes no xid; one backend means every producer txn has already committed, so nothing holds xmin down) and the settled_head fallback branch is unreachable in-browser; a produce lands in the NEXT claim (1,2] with no wait-a-tick caveat, two claims on one cursor come back disjoint (0,2]/(2,3], caught up reads low == high; the fence can be explained but never shown firing (needs a second backend holding a write open); spike SQL was pasted not extracted and deleted after answering — the drift-checked extraction rides with the tick step
- 0586 2026-08-24 accepted (pre-v1) — the sandbox's Tick runs the real consume path (getGroup → freshClaimMessagesWithCursor snapshot+gate → claimMessages lease → readMessages → commit's lease DELETE), with the handler-and-loop in the console and the statements extracted byte-exact; drift is now counted per `-- vulkan: <owner>` tag instead of per file, so a verb the site never runs (deleteGroup, partialCommit) needs no case while a statement added to a mirrored verb still fails; BatchLimit default 1 + the 37.1s composed lease make one tick one id; commit writes nothing else because the demo topic's delivery_log_mode is the default `failures` and successes are never collected — deliveryStatement/logStatement/partialCommit stay unextracted until the deferred fail-the-next-message toggle; the build surfaced unintended compaction (six keyed seed rows sharing `order-42` made five ticks read `· 0 messages`), fixed by giving each order its own compaction key — compaction as a taught concept waits for the deferred `delivery_1` panel
- 0587 2026-08-24 accepted (pre-v1) — sandbox consumers auto-run on their own clocks: the per-card Run button is REPLACED (user, not joined) by an auto-run toggle on by default, ~1s ± 150ms jitter so cards started together don't stay in lockstep, next run scheduled after the previous resolves (never setInterval); timers in a plain-TS AutoRunner (no reactive state, so vitest can load it — fake-timer tests incl. stop-mid-run), ChromeButton gained `pressed: boolean | null` (null = not a toggle → no aria-pressed at all) instead of a parallel toggle component, on-state styled off `[aria-pressed='true']` not a second data- attribute, muted amber ground + dim lacquer; global in-flight lock deleted (disabled now means the database is unavailable); a throwing run turns its own auto-run off; Reset stops every clock before dropping the handle; supersedes the "No timer" half of the step-by-step point settled 2026-08-24, and single-stepping a consumer is no longer available
- 0588 2026-08-25 accepted (pre-v1) — the compatibility matrix is exported from the gate's own rule: `migrate.ClassifySchemaSupport` is the one home (assertVersionSupported became a switch over it; `migrate.Version(registry)` absorbed the duplicated `len(Registry)+1`), `tools/compatexport` walks build x database calling it per cell into `website/src/data/compat.json` and takes registries as PARAMETERS so versions that do not exist are testable (`migration_test.go`/schemagatelab precedent); export carries verdicts not steps — TS never compares a version to a floor; `just site-verify` regenerates + `git diff --exit-code`s it; component is static (no `client:`), corner cell EMPTY with self-labeling headers (Cluster API/IBM/Oracle precedent — stacked arrow labels and a rowspan/colspan header both built and rejected); grid is one cell pre-v1, fixture registry behind `-example` is REVIEW SCAFFOLDING to delete; export models clean upgrades only (gate reads the recorded log, latest-by-id)
- 0589 2026-08-25 accepted (pre-v1) — a declaration's diagnose part is SQL: `Diagnose(...)` chains an ordered set of `NewQuery(label, sql)` onto Err*/Event (chained not a fifth NewError param — half the declarations have nothing to look at; 18 of 53 carry queries, absence renders as absence); MUST be the WithDefaults mutate-and-return pattern, never With/Wrap copy (constructors register the pointer, every surface reads the registry back, so a copy renders nothing anywhere), second call panics; placeholders are `{attribute_name}` from the ## Logging registry with the SQL carrying its own quoting (bare in identifier position `delivery_{topic_id}`, quoted per column type in value position) — psql variables rejected, they cannot concatenate inside an identifier; SQL is a const beside the declaration not a datastore method (## SQL binds datastores because they EXECUTE); metrics declare none; three surfaces — `vulkan explain` renders after the block never folded in (CLI error block stays tight), site reads tools/codeexport → codes.json carrying the WHOLE record so the error pages' hand-copied frontmatter finally gets its parked drift check, doc comment gets a one-line pointer only because gopls hover renders the doc + TYPE but never the initializer; placeholders parsed once in Go, TS sqlPlaceholders deleted; made the prose attribute registry binding via a tools/conventions walk (found 7 pre-existing violations, `kind`→`owner_kind` + 3 new rows) and forced raise sites to attach ids they already knew; `vulkan explain --run` stays unbuilt but reachable
- 0590 2026-08-26 accepted (pre-v1) — a fix substitutes the caller's values: `Fix` takes the same `{attribute}` placeholders [0589] gave queries, filled from the raise's own attached values through `Error.Fill` (exported so the CLI fills its `cliFixes` rewrites too — "runs verbatim as pasted" stops being aspirational); `placeholder.go` is the vocabulary's one home so error.go never grows a second parse; value goes in RAW, the surrounding text carries its own quoting (`register "{cron_job}"` prose, `'{topic}'` SQL); unfilled renders LITERALLY and a tools/conventions walk over every `return <declared Err>` keeps one from reaching a reader. THE RULE the build found: a fix placeholder must be attachable at EVERY raise site (one string serves all of them) — queries are exempt, an ordered SET can offer a name-keyed and an id-keyed alternative. Six rewordings approved, four shipped: VK0004/13/22/23; VK0005 + VK0014 REVERTED mid-build (3 + 1 raise sites resolve by id, `Owner.Name` is the owner attribute not the topic) and took an id-keyed query instead, which also closed [0589]'s gap where those sites raised a condition whose query named a value their line never carried. Site fills BY NAME, never parsing the line's grammar: three shapes tried (text handler, JSON, `Error()` one-liner), the one-liner strict because a problem line repeats attribute names (`topic not found` must not read "not"); `fillSegments` reads the quoting already around the blank — quoted doubles `'`, bare accepts only `[A-Za-z0-9_]` or stays a blank. Paste box REPLACES the search strip; one `$state` module across three islands (the `read-tracking` pattern). Composed example lines carry no values, so the ROADMAP example-values item is what makes it self-demonstrating.
- 0591 2026-08-26 rejected (pre-v1) — the sandbox's Postgres is NOT prefetched: built, measured, reverted on the code-to-gain ratio, not on a defect. The roadmap's premise was wrong twice — the sandbox already boots in `onMount`, and it sits at the fold under `client:visible`, so the `requestIdleCallback` it asked for would fire LATER, queued behind three other idle islands; the real delay is a serial module waterfall (island + Svelte runtime → `database.ts` → the 602 KB PGlite chunk) ahead of a 5.53 MB gzipped download across FOUR assets and a 10 MB wasm compile. The reverted build: a page-level `<script>` `compileStreaming`ing the three files on idle into `PGlite.create()`'s `pgliteWasmModule`/`initdbWasmModule`/`fsBundle` options (which it does skip its own download for — `startArtifactDownload` is not exported, those options are the seam). Cost that lost to two saved round trips: a `?url` import reaching into node_modules by RELATIVE path (the package's exports field does not name the files), the tree's first page-level `<script>` load-bearing on sitting above `<BoardLayout>` or Astro renders it at the end of the body, and a second place knowing how PGlite starts — holding a compiled 10 MB Module and a 6 MB Blob with its own retry rule. Never measured in a browser; reopening needs a real measurement, not another estimate. `<link rel="prefetch">` is not a cheaper way back (no cache headers set, double-fetches under `astro dev`). The `transition:persist` sibling is the better lever — ClientRouter is listed in the stack but not wired up, so PGlite's module caches die every navigation
- 0592 2026-08-26 accepted (pre-v1) — ClientRouter wired up, sandbox NOT persisted: `transition:persist` cannot do what the roadmap asked — Astro's swap matches the attribute on BOTH documents (`if (!newEl) continue`) and the sandbox is on `/` alone, so navigating away destroys it whatever directive it carries; what survives a soft navigation is the MODULE REGISTRY (`oldElement.replaceWith(newElement)` keeps `window`), so PGlite's module-level Response and compiled-Module maps make `<ClientRouter />` alone cache the expensive half — one line in BoardLayout.astro (the only layout owning a `<head>`) covers all 80 pages, and returning to `/` skips the 5.53 MB gzipped download + 10 MB wasm compile while still running instantiate/initdb/DDL/seed. Skipping those too needs `DatabaseState` hoisted to a module singleton AND the consumer cards/groups/nextConsumer moved with it (else a returning reader sees "no runs yet" and "billing's cursor is at 0" over a cursor at 8) — deliberately NOT taken, it relocates the state model and pins a live Postgres all session. Two fixes the change forced: `DatabaseState.close()` called from the sandbox's `onDestroy` (a full load used to release the 128 MB wasm memory for free, a soft navigation does not; `reset()` now routes through it), and OUR OWN reduced-motion guard for `::view-transition-*` in global.css base — Astro's `viewtransitions.css` only reaches the build when a `transition:*` directive is used, so with none the cross-fade is the browser default and nothing gates it. Price: +16.29 KB raw / 5.55 KB gzipped initial JS on EVERY page, earned by site-wide soft navigation not by the one sandbox. Teardown chain verified: `astro-island.disconnectedCallback` → `astro:unmount` → `@astrojs/svelte` `unmount(component)` → `onDestroy`. Page `<script>`s do not re-run across a swap without `data-astro-rerun`
- 0593 2026-08-26 accepted (pre-v1) — the night board, chosen from the footer: [0583]'s deferred dark mode ships as a second board style on `<html>` (`data-board-style`), donor = the era's dark-board convention (near-black ground, navy chrome, silver text) NOT the site's own volcano; amber is excluded from the swap on purpose — "amber means new-or-act and nothing else" is a [0583] rule, so the night sheet inherits the classic amber primitives and only darkens the grounds under them. A style = its own primitive sheet (`--surface-coal`, `--band-slate-start`, `--link-sky` — primitive names stay honest about their colour rather than one hue-named sheet lying under two palettes) + a re-point of the semantic names components already consume; structural tokens (grid, z-index, fonts) stated once. Zero raw colours across 42 component stylesheets meant NO component changed. Shiki is the one thing tokens cannot reach — it bakes colours at build time, so both themes ship with `defaultColor: false` and the base layer reads `--shiki-light`/`--shiki-dark` (recorded in website/CONVENTIONS.md as the one theme-branch outside the tokens layer; `font-style`/`font-weight` become variables too, easy to miss). Pre-paint `is:inline` script in BoardLayout's `<head>`, re-applied on `astro:after-swap` because ClientRouter [0592] copies the incoming document's `<html>` attributes and the static build carries none; it re-reads storage each time so a footer choice survives navigation. `prefers-color-scheme` SEEDS the first visit only — a stored choice outranks the OS for good, still not a system toggle. `BoardFooter` became a `client:idle` island (a `<select>` in static HTML does nothing): +1.6 KB raw / 0.8 KB gzipped. Contrast deliberately MATCHES classic rather than improving on it (ink-faint 3.50 in both, comment 3.68 vs 3.31, null cell 2.70 vs 1.99) — raising any of them is one change touching both sheets. Storybook takes the style as a toolbar global, not a second story per component
- 0594 2026-08-26 rejected (pre-v1) — initial-payload byte ceilings are NOT built: built green then reverted on the code-to-gain ratio, not on a defect (the [0591] judgement again). The reverted build was `website/scripts/initial-payload.mjs` (~240 lines) as the last step of `npm run build`: walk every built page for what the HTML names (script `src`, island `component-url`/`renderer-url`), close it over STATIC imports by regex over minified ESM, add inline script bytes, gzip each chunk to a per-page total; TWO ceilings across all pages naming none (heaviest = a new island anywhere, lightest = growth every reader pays) because the homepage is NOT where regressions land — ClientRouter [0592] (+16.29 KB) and the footer island [0593] (+1.6 KB) were paid on all 80 pages and a homepage ceiling absorbs both silently. Plus an 11-case vitest file, an eslint globals block, two rule-file edits — a lot of machinery, including a hand-written import-graph walk, behind one number that moves a few times a year and is visible in any build's chunk list. FACTS THAT SURVIVE: counting inline scripts (Astro's island bootstrap + hydration directives, [0593]'s pre-paint script) adds ~2.46 KB gz per page, so every earlier reading undercounted — the homepage is 34.76 KB gz / 88.15 KB raw, not 32.30 / 82.17; a browser measurement would have to define "initial" as a MOMENT (the sandbox boots PGlite in `onMount`, 5.53 MB / 4 assets) and a ceiling that depends on when counting stopped is flaky; Vite's manifest would have made the walk exact and short but Astro overrides `vite.build.manifest` and emits nothing — stop there. Dropping Playwright from the stack list was approved in the round and reverted with the rest (its only justification was that a written check had replaced it). Reviving needs a cheaper shape (an off-the-shelf checker that is one config file) or a reason beyond one number
- 0595 2026-08-26 accepted (pre-v1) — spacing token scale: the closed scale website/CONVENTIONS.md promised is the exact set in use — `--space-1`…`--space-34` (every integer 1–14, then 16/18/20/22/24/26/32/34, 22 steps), each named by its pixel value, one tier beside `--z-raised`, style-independent; NO snapping to a sparser ladder (behavior-preserving sweep, zero visual change — consolidation is a later design pass, now enumerable in one place); swept 36 component stylesheets + 3 layout scoped blocks + global base/utilities + one story inline style; `/^margin/` `/^padding/` `/gap$/` join declaration-strict-value with `0`/`auto` in ignoreValues (sabotage-checked: catches raw px in mixed shorthands and logical properties); positioning coordinates, border widths and sizes stay raw px — drawing and sizing, not spacing; the one negative use is `calc(-1 * var(--space-34))`; layout `.astro` blocks sit outside the `src/**/*.css` glob — review carries them
- 0596 2026-08-26 accepted (pre-v1) — the decision records publish as a board: a Decision records board in the Troubleshooting shape — generated index thread at /decisions/, one thread per record at /decisions/NNNN/, all 335 records. A second glob collection reads ../docs/decisions IN PLACE (record number = collection id); the site adapts to the records, never the reverse: H1 parsed as the thread title and removed at render, vale/remark-lint still skip them (exception recorded in website/CONVENTIONS.md ## Content). Board machinery widened from CollectionEntry<'docs'> to a neutral Thread shape both collections adapt into (_board/threads.ts) — rows, stats, prev/next, what's-new, Pagefind ride along. [NNNN] citations linkify via one remark plugin gated to the records directory (no-match stays literal; numbers have gaps), registered as markdown.processor: unified({remarkPlugins}) since Astro 7 deprecates markdown.remarkPlugins. The index is decisions.mdx + a record-index component building rows from the collection — a computed grid, not generated prose. 11 records (0558–0568) had bare metadata lines normalized to the declared frontmatter; decision prose untouched — format-defect repair is not an edit of the decision
- 0597 2026-08-27 accepted (pre-v1) — website layered error handling: four severity tiers built as four separate rungs — (1) inline at the source (the existing `errorMessage: string | null` + `role="alert"` pattern extended to every uncovered call site; `<svelte:boundary>` cannot catch DOM handlers/timers/async work, so call-site try/catch stays the workhorse), (2) shared `<svelte:boundary>` failed-snippet component fallback plus a dedicated read of the sandbox's boot-failure status (storage denial degrades to in-memory + messaging, never a dead console), (3) ONE site-notice component fed by 'error' + 'unhandledrejection' + 'vite:preloadError' registered once in a bundled layout script (survives ClientRouter swaps) — banner default, modal ONLY for the stale-chunk-after-redeploy must-acknowledge case (reload-once behind a sessionStorage guard), error toasts banned, (4) the same notice's full-page state as last resort, not a route (static output has no 500; 404.html also keeps Cloudflare Pages out of SPA-fallback mode). The message split: reader-typed SQL shows the real Postgres error verbatim (the console is a terminal — the error is the content); site machinery speaks the house problem+fix grammar. website/CONVENTIONS.md gains ## Errors; every failure state is a story. Rejected: modal-as-default, a /broken route
- 0598 2026-08-27 accepted (pre-v1) — the site notice's full-page face is cut until something needs it (narrows [0597] tier 4): the shell is prerendered so prose always renders — no detectable "nothing below works" signal exists, and every candidate trigger would cover readable content with an opaque overlay; SiteNoticeKind = 'banner' | 'modal', the modal's reload carries the last-resort role; built + storied then cut in the same round (~15 lines); revival needs a real trigger (a JS-dependent page or a failure class that provably blanks the shell) and a new record
- 0599 2026-08-27 accepted (pre-v1) — the doc site's cookie notice IS its privacy note: the site sets no cookies (no analytics, no tracker, no third party; only localStorage the reader's own browser holds), so the one piece of chrome every reader is trained to look at becomes the place that says so. Two acts in one bottom-edge component (`src/components/cookie-notice/`, `client:idle` in BoardLayout). Act one is a faithful copy of the compliance-vendor standard — "we value your privacy", the cookies-and-similar-technologies paragraph, three EQUALLY prominent buttons (Accept all / Reject non-essential / Manage preferences) and a cookie-policy control — and every one of them reaches the same act two, so which was pressed changes nothing. Act two is the real privacy statement under a headline drawn from a five-entry pool (`reveals.ts`); only the headline and two button labels rotate, the statement is one fact and never varies. Each act-two button carries TWO lines — the joke the reader reads, and beneath it the plain action being pressed ("star the repo", "dismiss") — so the accessible name holds the visible label and the real action both and the bit costs nothing in clarity. THE RULE: its own surface, NEVER site-notice — site-notice is the shipped error channel [0597] and a prank on it would teach readers to ignore the one banner that means something actually broke; the two share no component, state module, or screen edge. First visit, one answer per browser stored at the moment act one is answered, no veil and no focus trap (a real consent bar does not block the page). Rejected: firing on a return visit instead (funnier against the "you last visited" bar, but most readers would never see it); a modal veil. Act two's paragraph is load-bearing prose — a change to what the site stores changes it in the same edit
- 0600 2026-08-27 accepted (pre-v1) — each consent control gets its own answer, and Accept all gets its own component (narrows [0599]): the five-entry randomized pool is GONE — a random headline throws away the punchline that actually lands, which is the one answering the button the reader pressed. `answers.ts` is a `Record<ConsentButton, ConsentAnswer>` as a DISCRIMINATED UNION, `{face:'modal'} | {face:'bar'; content: Answer}`, so the modal answer carries no content and the compiler refuses any read of one (it caught a stale reference the moment it was introduced); adding a control is a compile error until its entry exists. Three controls, not four — the Cookie policy link was cut. Reject/Manage rewrite the bottom bar in place via the shared cookie-answer component; Accept all gets accept-all-modal, its OWN component with its own veil/box/copy/stylesheet — the three-way split was forced by the ~80-line stylesheet rule, not by differing shapes. Accept all's consequence: the page stutters first (helpers/page-stutter — hard opacity blinks in the base layer plus scroll jolts, modal opens on animationend); THE SHOVE IS A SCROLL, NEVER A TRANSFORM, because a transform on the page becomes the containing block for every position:fixed child and would fling the consent bar to the document bottom mid-animation; the animationend listener checks event.target since a descendant's animation ending would otherwise cut the stutter short. Then a routing + account number type out a digit at a time over a dark `--color-hacked-veil` with memes pasted around the box — numbers are noise per opening but the SHAPE is real (9-digit routing with valid ABA checksum and genuine Fed district prefix, 12-digit account; a wrong length reads as a prop), 5 vitest cases. memes.ts declares each file once (natural size + whether it takes the card border) then places it as many times as it appears. Images load ONLY when the modal renders, confirmed by Chrome net log — a plain page load requests neither; the island chunk carrying the modal's markup is 3.4 KB gzipped on every page, accepted as small enough to leave static. pixel-skull built then deleted when the memes replaced it. OPEN: the meme art is Nickelodeon's and sits in website/public/, local preview only — must be swapped for CC0/CC-BY before the site ships
- 0601 2026-08-27 accepted (pre-v1) — doc site versioning: the apex serves exactly one version and a release deploys the same build twice — `just site-deploy` (live) + `just site-freeze <slug>` to a permanent Cloudflare Pages branch alias never deployed to again (the React/Vue frozen-archive model; in-source snapshot copies and build-per-ref machinery rejected). No build carries the version list: public/versions.json on the live origin is the one registry, fetched at read time by every deployment (CORS via public/_headers), so old deployments grow the dropdown and the "you are reading old docs" band without redeploys. A build carries only its own stamp (site.ts docsVersion, `main` pre-release); version-band island + version-select component render the switcher, every page canonicals to the live origin, and Cloudflare already serves x-robots-tag noindex on alias domains
- 0602 2026-08-27 accepted (pre-v1) — doc site mobile pass: two declared breakpoints in website/CONVENTIONS.md ## CSS (640px layout collapse, 761px sandbox gate); the homepage's intro+sandbox post becomes a route-local component hydrated `client:media="(min-width: 761px)"` and CSS-hidden below it, so PGlite (~16MB) is never fetched on mobile — no replacement copy, a planned hero section is the all-widths answer; `client:media` joins the sanctioned island triggers; cookie notice hidden below 640px (the joke stays desktop); touch-target and 16px-input corrections under `pointer: coarse`, not width; z scale gains a step so site-notice outranks the consent bar. Rejected: CSS-hiding under client:visible (IntersectionObserver on a boxless target), replacement mobile copy, maximum-scale clamping
- 0603 2026-08-27 superseded by [0604] (pre-v1) — the page-failure net ignores browser cancellations (narrows [0597]): the unhandledrejection listener skips DOMExceptions named AbortError or InvalidStateError — an aborted view transition (any mid-transition viewport resize, e.g. a phone's URL bar collapsing) rejects the router's un-awaited promise while the navigation completes, and the banner was reporting the cosmetic cross-fade as a page failure; branch on the exception's name never its text; console output untouched. Rejected: message-text matching, swap-timing heuristics
- 0604 2026-08-27 accepted (pre-v1) — the transition's ready promise is caught at the source (supersedes [0603]): Chrome skips same-document view transitions on mobile (Chromium regression 456078987, also under the devtools emulator) and leaks the `ready` rejection the spec says to mark handled; Astro's router never touches `ready` (upstream #10830 closed unreproduced), so listenForPageFailures adds `event.viewTransition.ready.catch(() => {})` on astro:before-swap — Nuxt's own router fix (PRs #34515/#35537) applied at Astro's event seam — and the unhandledrejection net returns to fully strict, [0603]'s name filter removed. Deletable when Astro catches ready itself or Chrome implements mark-as-handled here
- 0605 2026-08-28 accepted (pre-v1) — the member profile page: /members/brandon/ renders the phpBB "Viewing profile" shape and thread-post's author name + avatar link to it (`/members/{author}/`); every fact real or absent — Total posts = siteThreads length, Website = repositoryUrl, Joined = firstCommitDate() added to the existing commit-log walk (the newest-first walk's last date IS the first commit's, no second git call); the "Personal text" strip (SMF's field name; Signature, persona fields, and Marquee/MOTD rejected) is the Stanley Parable "the end is never " looping seamlessly (two identical copies sliding translateX(-100%), second copy aria-hidden, motion behind prefers-reduced-motion, all lowercase — an endless sentence has no start); member-profile + member-personal-text are route-local components (placement law, split by the ~80-line stylesheet rule, no stories outside the Storybook glob). Rejected: in-place easter egg, typed-out loop
- 0606 2026-08-28 accepted (pre-v1) — the doc site's browser support line: two declared lines in website/CONVENTIONS.md ## Browser support — supported = Baseline Widely Available (Chrome/Edge 121+, Firefox 123+, Safari/iOS 17.2+; 87.0% of tracked global usage 2026-08, more than browserslist `defaults` at 84.7%, whose extra budget goes to Chrome 109/UC Browser/Opera Mini traffic a developer doc site rarely sees), readable = the build floor pinned as `vite.build.target` in astro.config.mjs (Vite 8's baseline-widely-available list: chrome111/edge111/firefox114/safari16.4/ios16.4 — pinned because the default floats per Vite major and had already moved once silently). Enhancements above the supported line ship only with the fallback they already carry (view transitions → instant swap; requestIdleCallback → setTimeout; PGlite sandbox → boot notice). Below the floor: CSS error recovery only, no @supports/polyfills/legacy bundle; the structural cliff (@layer, :where(), :focus-visible) is Safari 15.4/Chrome 99, under 0.2%. Rejected: browserslist defaults, Baseline Newly Available (76%), compat lint tooling as a standing gate
- 0607 2026-08-28 accepted (pre-v1) — the sandbox quiesces before closing its database: the [0606] spot check surfaced that leaving a booted sandbox could freeze the next page for good — PGlite's synchronous wasm spins forever (execProtocolRawSync inside pgl_setPGliteExitStatus) when a statement reaches an exited Postgres, and Svelte teardown can flush a panel's run after onDestroy's close; the old close also nulled `connecting` first, so a late connect() could boot a second unclosable 128 MB database. Fix: every statement-running verb goes through one private `perform` that registers the operation in `pendingOperations` BEFORE its first await (an operation parked on `await connect()` was invisible to the first attempt's drain and the freeze survived it); close() sets `closing` (new operations get a rejected promise the caller's own error path handles), drains with allSettled in a loop, then shuts down; `closing` clears so reset() reconnects. Playwright flow test pins the regression in all three engines. PGlite's own close-with-statement-in-flight deadlock (repro'd in Node, 0.5.6 and 0.5.8) remains upstream
- 0608 2026-08-28 accepted (pre-v1) — Playwright covers the editor swap and the initial-JS ceiling: two flow tests close the [0607] gaps — the editor test waits for .cm-editor over the static shell, types, and asserts the panel chip leaves "auto re-runs" (proof the swap wired setSql); the byte test sums script response bytes on the homepage at a 640px viewport, below the 761px sandbox gate where the island never hydrates, so networkidle is a stable moment ([0594]'s "initial is a moment" objection answered by measuring where nothing more ever loads) — raw bytes, measured 82,080, ceiling 96,000, plus an assertion that no PGlite chunk is requested. website/CONVENTIONS.md ## Islands & loading reworded to describe this check; the import-graph walk stays rejected [0594]. Accepted blind spot: a CodeMirror static import inside the sandbox island is invisible below the gate
- 0609 2026-08-28 accepted (pre-v1) — website/VOICE.md, the doc site's prose voice file, loaded via website/CLAUDE.md and binding site prose only: ~15 verbatim author samples (typos kept; standing instruction to correct spelling and reproduce everything else), two contrastive same-passage pairs with differences named, measurable rules, and an anti-slop revision checklist run as its own pass (generation-time bans decay). Built on the validated evidence order: samples > descriptions, flat past ~10 samples, contrastive pairs > samples alone; fine-tuning rejected as workflow-incompatible. Voice target = the samples' structure and stance with spelling corrected. Later rungs on ROADMAP: author-seeded drafts, mining draft→published diffs back into the file
- 0610 2026-08-28 accepted (pre-v1) — example attribute values on code threads: a shared site-side value table (exampleValues in src/pages/_thread/example-line.ts, one value per log-attribute-registry name, per-code overrides for mirrored declarations like VK0023, missing name throws at build) composes each thread's example line faithful to the real renderers — the Error() one-liner with the fix filled, the text-handler event line — so pasting the example fills the thread's own queries and fix; the invariant runs as a round-trip test over codes.json. Maximal demonstration chosen: the line carries all fillable names even where a real raise attaches one. LogLine's blank-marking path and markPlaceholders deleted as dead. Rejected for now: per-page frontmatter values (the next rung), registry-side example values
